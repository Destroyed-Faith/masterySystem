# Tree power spec audit

Total findings: **0** (embedded `NewArtifactPowerData` only; legacy `PowerDefinition` rows are skipped).


No issues matched the current rules (lowercase keys, no type/value on specials, AoE sizeM/radiusM, mechanics triggerLimit / dual gates, etc.).
